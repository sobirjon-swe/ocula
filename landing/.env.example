# Backend API manzili — server tomonda chaqiriladi (Server Component,
# Route Handler), brauzerda emas, shuning uchun CORS kerak emas
# (BOSQICH-11.md).
BACKEND_API_URL=http://localhost:8000/api/v1

# Sitemap/robots va Open Graph metadata uchun — production domeni.
SITE_URL=https://optika.uz
